# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local credentials — never commit real values (.env.example is the template)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Throwaway browser-test screenshots (kept in the scratchpad, never the repo)
/*.png

# Generated from git history by `npm run changelog` (runs on predev + prebuild).
# Kept OUT of the repo because every build rewrites it — committing it made the
# build dirty the working tree and block the next `git pull` on the server.
src/data/changelog.json
